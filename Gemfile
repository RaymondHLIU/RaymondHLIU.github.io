source "https://rubygems.org"

# Keep local builds aligned with GitHub Pages' managed dependency set.
gem "github-pages", group: :jekyll_plugins

# Ruby 3 no longer bundles WEBrick, which Jekyll uses for local preview.
gem "webrick", "~> 1.8"
